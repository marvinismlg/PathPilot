import ComingSoonPage from "../../../components/ComingSoonPage";

export default function DashboardPage() {
  return (
    <ComingSoonPage
      title="Student Dashboard"
      description="Login now routes here instead of a missing `/student/page` path, so successful sign-in lands on a real screen."
    />
  );
}
