import ComingSoonPage from "../../../components/ComingSoonPage";

export default function PortalProfilePage() {
  return (
    <ComingSoonPage
      title="Profile"
      description="This route is set up as a valid page module so the App Router can compile without route validator errors."
    />
  );
}
