import ComingSoonPage from "../../components/ComingSoonPage";

export default function ProfileResultsPage() {
  return (
    <ComingSoonPage
      title="Profile Results"
      description="This placeholder keeps the route available without leaving an empty `page.tsx` for Next.js to choke on."
    />
  );
}
